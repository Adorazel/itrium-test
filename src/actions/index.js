import {setRequestUrl, setRequestMethod, setRequestHeaders, setRequestBody} from "./request.action"
import {fetchUrl} from "./response.action"

export {
  setRequestUrl,
  setRequestMethod,
  setRequestHeaders,
  setRequestBody,
  fetchUrl
}